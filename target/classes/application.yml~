spring:
    application:
        name: SpringBootAngular
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://localhost:3306/book
        username: root
        password: 12345
    jpa:
      database-platform: org.hibernate.dialect.MySQL5Dialect
      hibernate:
        ddl-auto: create-drop
      show-sql: true
server:
    port: 8080
